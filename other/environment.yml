name: ptychopinn_torch
channels:
  - pytorch        # Highest priority for PyTorch packages
  - nvidia         # For CUDA toolkit, cudnn
  - conda-forge    # For a wide range of other packages

dependencies:
  # --- Core Python & PyTorch ---
  - python=3.10     # Or 3.9, 3.11, as per your needs
  
  # PyTorch with CUDA 11.8 support.
  - pytorch::pytorch
  - pytorch::torchvision
  - pytorch::torchaudio
  - pytorch-cuda=11.8 

  # --- Your Other Essential Packages ---
  - scikit-learn
  - pytorch-lightning
  - jupyterlab       
  - matplotlib
  - pandas            
  - h5py
  - scikit-image
  - mlflow           
  - opencv

  # --- Pip dependencies (if absolutely necessary) ---
  - pip
  - pip:
    - -r requirements.txt  
    - -e .                 
